{
  "name": "@piwork/pi-host",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Node process that embeds the Pi SDK inside the container and bridges it to the shell over JSONL stdio.",
  "bin": {
    "pi-host": "./src/index.ts"
  },
  "scripts": {
    "verify:pi": "sh scripts/verify-pi.sh",
    "verify:builtins": "sh scripts/verify-builtins.sh"
  },
  "dependencies": {
    "@earendil-works/pi-coding-agent": "0.84.0"
  }
}
