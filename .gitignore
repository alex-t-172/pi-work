node_modules/
dist/
*.tsbuildinfo
*.log
.DS_Store

# ephemeral spike/smoke/delivery workspaces (created under the repo so Rancher shares them)
.spike-tmp-*/
.smoke-tmp-*/
.deliv-tmp-*/

# local env / secrets — never commit
.env
.env.*
*.local
*.pem
*.key

# editor / OS cruft
.idea/
*.swp
Thumbs.db

# Note: project MCP connectors live in .pi/mcp.json and are intentionally trackable
# (shareable, like .mcp.json). The shell refuses to write token secrets there — those go
# to host-side global config. If a repo's .pi/mcp.json ever holds a secret, ignore it here.

# Claude Code agent worktrees / scratch
.claude/

# internal shipping/OSS plan — local dev artefact, not for the public repo
docs/shipping-oss-plan.md
